<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.ssafy.project.model.mapper.MemberMapper">


	<resultMap type="memberDto" id="member">
		<result column="user_id" property="userId" />
		<result column="user_name" property="userName" />
	</resultMap>

	<!-- ADMIN -->
	<resultMap type="memberDto" id="memberAll" extends="member">
		<result column="user_password" property="userPwd" />
		<result column="email" property="email" />
		<result column="sido_code" property="sidoCode" />
		<result column="create_at" property="joinDate" />
		<result column="delete_at" property="deleteAt" />
	</resultMap>

	<select id="idCheck" parameterType="String" resultType="int">
		select
		count(user_id)
		from members
		where user_id = #{userId}
	</select>

	<insert id="joinMember" parameterType="memberDto">
		insert into members
		(user_name, user_id, user_password, email, sido_code)
		values
		(#{userName}, #{userId}, #{userPwd}, #{email}, #{sidoCode})
	</insert>

	<select id="loginMember" parameterType="map" resultMap="member">
		select
		user_id, user_name
		from members
		where user_id = #{userId} and
		user_password = #{userPwd} and delete_at is null
	</select>

	<!-- ADMIN -->
	<select id="listMember" resultMap="memberAll">
		select user_id, user_name, user_password, email, sido_code, create_at, delete_at
		from members
	</select> <!-- 임시 -->

	<select id="getMember" parameterType="string"
		resultMap="memberAll">
		select user_id, user_name, user_password, email, m.sido_code, s.sido_name, create_at, delete_at
		from members m, sido s
		where m.sido_code = s.sido_code 
		and user_id = #{userId}
	</select>

	<update id="updateMember" parameterType="memberDto">
		update members
		<trim prefix="set" suffixOverrides=",">
			<if test="userName != null">user_name = #{userName},</if>
			<if test="userPwd != null">user_password = #{userPwd},</if>
			<if test="email != null">email = #{email},</if>
			<if test="sidoCode != null and sidoCode != 0">sido_code = #{sidoCode},</if>
		</trim>
		where user_id = #{userId}
	</update>

	<delete id="deleteMember" parameterType="String">
		delete from members
		where user_id = #{userId}
	</delete>

</mapper>